---
import { getLangFromUrl } from "../i18n/utils";
import Navbar from "../components/common/Navbar.astro";
import Footer from "../components/common/Footer.astro";
import WhatsAppButton from "../components/ui/WhatsAppButton.astro";
import Atmosphere from "../components/ui/Atmosphere.astro";

import { ClientRouter } from 'astro:transitions';
import "../styles/global.css";

interface Props {
	title: string;
	description?: string;
	hideBookingButtons?: boolean;
}

const { title, description, hideBookingButtons = false } = Astro.props;
const lang = getLangFromUrl(Astro.url);
--- 

<!doctype html>
<html lang={lang} class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
        
        <!-- SEO / Open Graph -->
        <meta name="description" content={description || "Luxe vakantiewoningen 'De Oude Schuur' & 'De Peerdestal' in Otterlo, direct aan de Hoge Veluwe. Authentiek overnachten met modern comfort."} />
        <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
        <meta property="og:type" content="website" />
        <meta property="og:url" content={Astro.url} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description || "Luxe vakantiewoningen in Otterlo. Geniet van rust, ruimte en luxe bij Boerderij Voorste Eng."} />
        <meta property="og:image" content={new URL("/images/real/exterior/exterior-6.avif", Astro.url)} />
        
        <!-- Twitter -->
        <meta property="twitter:card" content="summary_large_image" />
        <meta property="twitter:url" content={Astro.url} />
        <meta property="twitter:title" content={title} />
        <meta property="twitter:description" content={description || "Luxe vakantiewoningen in Otterlo. Geniet van rust, ruimte en luxe bij Boerderij Voorste Eng."} />
        <meta property="twitter:image" content={new URL("/images/real/exterior/exterior-6.avif", Astro.url)} />

        <ClientRouter />
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Outfit:wght@300;400;500;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
	</head>
	<body class="font-body bg-stone-50 text-stone-800 antialiased flex flex-col min-h-screen relative">

		<Navbar lang={lang} />
		<main class="flex-grow z-10 relative">
			<slot />
		</main>
		<Footer lang={lang} />
		{!hideBookingButtons && (
			<!-- Sticky 'Book Now' Button (Mobile Only) -->
			<div class="fixed bottom-4 left-4 z-50 md:hidden pointer-events-none">
				<a id="book-now-btn" href={`/${lang}/boeken`} class="bg-bordeaux hover:bg-bordeaux-dark text-white font-bold py-3 px-6 rounded-full transition duration-300 shadow-lg transform hover:scale-105 text-sm uppercase tracking-wide pointer-events-auto block border-2 border-transparent">
					{lang === 'nl' ? 'Nu Boeken' : (lang === 'de' ? 'Jetzt Buchen' : 'Book Now')}
				</a>
			</div>
		)}

        <Atmosphere />
        {!hideBookingButtons && <WhatsAppButton />}

        <script>
            import Lenis from 'lenis'

            // Smooth Scrolling
            const lenis = new Lenis({
                duration: 1.2,
                easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
                direction: 'vertical',
                gestureDirection: 'vertical',
                smooth: true,
                mouseMultiplier: 1,
                smoothTouch: false,
                touchMultiplier: 2,
            })

            function raf(time) {
                lenis.raf(time)
                requestAnimationFrame(raf)
            }

            requestAnimationFrame(raf)

            // Dynamic Button Color on Footer Intersection
            const observer = new IntersectionObserver((entries) => {
                const button = document.querySelector('#book-now-btn');
                
                if (button) {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            // Footer is visible - Turn button white
                            button.classList.remove('bg-bordeaux', 'text-white', 'hover:bg-bordeaux-dark');
                            button.classList.add('bg-white', 'text-bordeaux', 'hover:bg-stone-100');
                        } else {
                            // Footer is hidden - Revert to red
                            button.classList.remove('bg-white', 'text-bordeaux', 'hover:bg-stone-100');
                            button.classList.add('bg-bordeaux', 'text-white', 'hover:bg-bordeaux-dark');
                        }
                    });
                }
            }, {
                root: null,
                threshold: 0.1 // Trigger when 10% of footer is visible
            });

            const footer = document.querySelector('footer');
            if (footer) {
                observer.observe(footer);
            }
        </script>
	</body>
</html>
